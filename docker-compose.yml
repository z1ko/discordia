version: '3.0'

services:
    database:
        container_name: database
        image: redis
        command: redis-server --appendonly yes
        volumes: 
            - "./redis:/data"
        ports:
            - "6379:6379"
